<?php

/* translators: %s: site name. */
$title = sprintf( __( 'Welcome to %s', 'emrikol' ), $name );

/* Translators: %s: user name. */
$hello = sprintf( __( 'Hello %s', 'emrikol' ), $user );

/* translators: 1: post count, 2: site name. */
$body = sprintf( _n( '%1$d post on %2$s', '%1$d posts on %2$s', $count, 'emrikol' ), $count, $name );

/* not a translators note. */
$other = 1;

/*translators: no space after the opener. */
$edge = 2;
